@model USDA.ARS.GRIN.GGTools.Taxonomy.ViewModelLayer.FamilyViewModel
<div class="form-group">
    <label>Type Genus</label>
    <div class="input-group">
        <span id="placeholder-genus-name" style="background-color: #ecf0f5; border: 1px solid #ced4da; width: 100%; position: relative; -ms-flex: 1 1 auto; flex: 1 1 auto; min-width: 0; margin-bottom: 0; padding: 0.375rem 0.75rem; height: calc(2.25rem + 2px); position: relative; -ms-flex: 1 1 auto; flex: 1 1 auto; width: 1%; min-width: 0; margin-bottom: 0;">@Html.Raw(Model.Entity.TypeGenusName)</span>@Html.HiddenFor(x => x.Entity.TypeGenusID)
        @Html.HiddenFor(x => x.Entity.TypeGenusID)
        <span class="input-group-append">
            @if (Model.Entity.TypeGenusID > 0)
            {
                <a href="@Url.Action("Edit", "Genus", new { @entityId = Model.Entity.TypeGenusID })" class="btn btn-default btn-flat">
                    <i class="fa fa-link"></i>
                </a>
            }

        </span>
        <span class="input-group-append">
            <button id="btnOpenGenusLookupModal"
                    type="button"
                    data-toggle="modal"
                    data-ggtools-action="TypeGenusID"
                    data-ggtools-value="TypeGenusName"
                    data-target="#modal-genus-lookup"
                    class="btn btn-default btn-flat">
                <i class="fa fa-search"></i>
            </button>
        </span>
    </div>
</div>